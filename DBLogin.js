#EVENT trigger DB_RESPONSE Assignment as needed

var dbuser = ['root','sa','sys','system'];
var i;
for (i=0; i < dbuser.length; i++) {
  switch(dbuser) {
	case dbuser[i];
		//DB.commitRecord();
		debug('dbuser: ' + dbuser[i] + '/n'
			'db length: ' + dbuser length);
		break;
	}
}
